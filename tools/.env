ROOT_DIR=/Users/stuartcw

# Directories to skip during scanning
#i Note: These are combined with built-in defaults
# Format: comma-separated, no spaces
STOP_DIRS=node_modules,.git,venv,pycache,.idea,.vscode,build,dist,.pytest_cache,.mypy_cache,.tox,.coverage,htmlcov,.env,.venv,env,.DS_Store,.pyc,.pyo,.pyd,.Python,.so,target,vendor,bin,obj,out,logs,.next,.nuxt

OPEN_ON_CREATION=True
